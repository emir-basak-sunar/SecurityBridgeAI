# CF production deps (minimal — aicore/groq providers)
requests>=2.31.0
tenacity>=9.0.0
thefuzz>=0.22.0
python-dateutil>=2.8.0
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=22.0.0

# Local-only extras: pip install -r requirements-dev.txt
